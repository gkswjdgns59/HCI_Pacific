import React, { Component } from 'react';

class Header extends Component{
    render(){
        return (
        <header>
        This is header
        </header>
        );
    }
}

export default Header;